# Exercices — Path-specific and review

## Exercice — Path-specific instructions

1. Create `.github/instructions/source.md` avec `applyTo: "**/*.py"`.
2. Create `.github/instructions/tests.md` avec `applyTo: "**/test_*.py"`.
3. Agent mode: "Fix lint errors in src/" then review diff.
4. Generate a Conventional Commits message via Copilot.

> **Solution** : [Accéder à la correction](../correction/en/correction-05-path-specific-review.md)
